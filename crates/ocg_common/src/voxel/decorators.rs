//! The builtin biome decorator types.
//! Most of this will be moved to a "base" mod at some point in the future.

use ocg_schemas::voxel::biome::decorator::BiomeDecoratorRegistry;

pub fn setup_basic_decorators(registry: &mut BiomeDecoratorRegistry) {}
